# 配套 UI（可选）

模块单独可用（看 txt / 31338）。

若要用悬浮窗：

- **方案 A**：继续用原版 JCC Controller 的 UI，**不要点注入**，只连 `127.0.0.1:31338`（需改源码跳过 inject；或等我们出薄壳 APK）  
- **方案 B**：等本目录后续 `ShellApp` 构建产物  

当前优先保证 Zygisk 模块扫出 `jcc_cardpool.txt`。
